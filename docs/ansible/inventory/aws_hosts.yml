all:
  children:
    aws_ec2:
      hosts:
        opspilot_production:
          ansible_host: YOUR_EC2_PUBLIC_IP_OR_DNS
          ansible_user: ubuntu
          ansible_ssh_private_key_file: "~/.ssh/opspilot-aws.pem"
